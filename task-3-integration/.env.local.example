# Counter contract address (from Task 1 deploy). Required for connect / increment.
NEXT_PUBLIC_COUNTER_CONTRACT_ADDRESS=

# Task 2 API base URL (for "Refresh from API")
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
