const hre = require("hardhat");

async function main() {
  // TODO: Deploy Counter and log the contract address
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
