# RPC URL for Ethereum-compatible network (e.g. Hardhat: http://127.0.0.1:8545)
RPC_URL=http://127.0.0.1:8545

# Counter contract address (deploy via Task 1, then paste address here)
COUNTER_CONTRACT_ADDRESS=

# Optional: port for the API (default 3000)
PORT=3000
