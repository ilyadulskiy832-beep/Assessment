/**
 * Minimal ABI for Counter contract (getCount only).
 * For full ABI use Task 1 artifacts: artifacts/contracts/Counter.sol/Counter.json
 */
module.exports = [
  "function getCount() view returns (uint256)",
];
